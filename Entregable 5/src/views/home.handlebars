<div>
    <h1>Lista de productos agregados hasta el momento</h1>
    <div id="item-container">
        {{#each docs}}
        <div class="product-card">
            <p>id: {{this._id}}</p>
            <p>Código: {{this.code}}</p>
            <p>{{this.title}}</p>
            <p>Categoría: {{this.category}}</p>
            <p>{{this.description}}</p>
            <p>$ {{this.price}}</p>
            <p>Stock: {{this.stock}} unidades</p>
        </div>
        {{/each}}
    </div>
</div>